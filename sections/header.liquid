<header class="site-header" id="site-header">
  <div class="container">
    <div class="header-inner">

      <!-- Logo -->
      <div class="header-logo">
        <a href="/">
          <div class="logo-wrapper">
            <div class="logo-icon">
              <svg width="40" height="40" viewBox="0 0 40 40" fill="none" xmlns="http://www.w3.org/2000/svg">
                <circle cx="20" cy="20" r="20" fill="#1A2B4A"/>
                <ellipse cx="20" cy="28" rx="6" ry="3" fill="#00C9C8" opacity="0.3"/>
                <rect x="17" y="8" width="6" height="18" rx="3" fill="#00C9C8"/>
                <circle cx="20" cy="8" r="3" fill="#F0C040"/>
                <circle cx="16" cy="18" r="1.5" fill="white" opacity="0.8"/>
                <circle cx="24" cy="22" r="1" fill="white" opacity="0.6"/>
                <circle cx="18" cy="25" r="1.2" fill="white" opacity="0.7"/>
              </svg>
            </div>
            <div class="logo-text">
              <span class="logo-kova">KOVA</span>
              <span class="logo-goods">GOODS</span>
            </div>
          </div>
        </a>
      </div>

      <!-- Navigation -->
      <nav class="header-nav" id="header-nav">
        <ul>
          <li><a href="/">Home</a></li>
          <li><a href="/collections/all">Shop</a></li>
          <li><a href="/pages/about">About</a></li>
          <li><a href="/pages/faq">FAQ</a></li>
          <li><a href="/pages/contact">Contact</a></li>
        </ul>
      </nav>

      <!-- Header Actions -->
      <div class="header-actions">
        <a href="/cart" class="cart-icon">
          <i class="fas fa-shopping-cart"></i>
          <span class="cart-count">{{ cart.item_count }}</span>
        </a>
        <button class="mobile-menu-btn" id="mobile-menu-btn">
          <i class="fas fa-bars"></i>
        </button>
      </div>

    </div>
  </div>
</header>

<!-- Mobile Menu -->
<div class="mobile-menu" id="mobile-menu">
  <div class="mobile-menu-header">
    <span>Menu</span>
    <button class="mobile-close-btn" id="mobile-close-btn">
      <i class="fas fa-times"></i>
    </button>
  </div>
  <ul>
    <li><a href="/">Home</a></li>
    <li><a href="/collections/all">Shop</a></li>
    <li><a href="/pages/about">About</a></li>
    <li><a href="/pages/faq">FAQ</a></li>
    <li><a href="/pages/contact">Contact</a></li>
  </ul>
</div>
<div class="mobile-overlay" id="mobile-overlay"></div>

<style>
.site-header {
  background: var(--color-white);
  position: sticky;
  top: 0;
  z-index: 1000;
  box-shadow: 0 2px 20px rgba(26,43,74,0.08);
  transition: var(--transition);
}
.site-header.scrolled {
  box-shadow: 0 4px 30px rgba(26,43,74,0.15);
}
.header-inner {
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: 16px 0;
}
.logo-wrapper {
  display: flex;
  align-items: center;
  gap: 10px;
}
.logo-text {
  display: flex;
  flex-direction: column;
  line-height: 1;
}
.logo-kova {
  font-size: 22px;
  font-weight: 800;
  color: var(--color-primary);
  letter-spacing: 3px;
}
.logo-goods {
  font-size: 11px;
  font-weight: 300;
  color: var(--color-aqua);
  letter-spacing: 5px;
}
.header-nav ul {
  display: flex;
  list-style: none;
  gap: 36px;
}
.header-nav ul li a {
  font-size: 14px;
  font-weight: 600;
  color: var(--color-primary);
  letter-spacing: 1px;
  text-transform: uppercase;
  transition: var(--transition);
  position: relative;
}
.header-nav ul li a::after {
  content: '';
  position: absolute;
  bottom: -4px;
  left: 0;
  width: 0;
  height: 2px;
  background: var(--color-aqua);
  transition: var(--transition);
}
.header-nav ul li a:hover::after {
  width: 100%;
}
.header-nav ul li a:hover {
  color: var(--color-aqua);
}
.header-actions {
  display: flex;
  align-items: center;
  gap: 20px;
}
.cart-icon {
  position: relative;
  font-size: 20px;
  color: var(--color-primary);
  transition: var(--transition);
}
.cart-icon:hover {
  color: var(--color-aqua);
}
.cart-count {
  position: absolute;
  top: -8px;
  right: -8px;
  background: var(--color-aqua);
  color: white;
  font-size: 10px;
  font-weight: 700;
  width: 18px;
  height: 18px;
  border-radius: 50%;
  display: flex;
  align-items: center;
  justify-content: center;
}
.mobile-menu-btn {
  display: none;
  background: none;
  border: none;
  font-size: 22px;
  color: var(--color-primary);
  cursor: pointer;
}
.mobile-menu {
  position: fixed;
  top: 0;
  right: -300px;
  width: 280px;
  height: 100vh;
  background: var(--color-primary);
  z-index: 9999;
  transition: right 0.3s ease;
  padding: 20px;
}
.mobile-menu.open {
  right: 0;
}
.mobile-menu-header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  color: white;
  font-size: 18px;
  font-weight: 700;
  margin-bottom: 30px;
  padding-bottom: 20px;
  border-bottom: 1px solid rgba(255,255,255,0.1);
}
.mobile-close-btn {
  background: none;
  border: none;
  color: white;
  font-size: 20px;
  cursor: pointer;
}
.mobile-menu ul {
  list-style: none;
}
.mobile-menu ul li {
  margin-bottom: 20px;
}
.mobile-menu ul li a {
  color: white;
  font-size: 16px;
  font-weight: 600;
  letter-spacing: 1px;
  text-transform: uppercase;
  transition: var(--transition);
}
.mobile-menu ul li a:hover {
  color: var(--color-aqua);
}
.mobile-overlay {
  display: none;
  position: fixed;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
  background: rgba(0,0,0,0.5);
  z-index: 9998;
}
.mobile-overlay.open {
  display: block;
}
@media (max-width: 768px) {
  .header-nav {
    display: none;
  }
  .mobile-menu-btn {
    display: block;
  }
}
</style>
